# Asari シェル - 仕様書

## 概要

Asariは学習目的で開発されるモダンなコマンドラインシェルである。
Rustの構文や型システムから影響を受けた設計を採用する。

---

# コマンド実行仕様

## 全体構文

Asariの入力は以下の階層構造で解析される。

```txt
CommandLine ::= Statement (';' Statement)* ';'? Comment?
Statement   ::= ShellCommand | EnvAssign | Pipeline
```

### CommandLine

1行の入力全体を表す。セミコロン `;` で区切られた複数のStatementと、末尾のオプショナルなコメントで構成される。

### Statement

Statementは3種類に分類される。パース時は以下の順序で試行される。

1. **ShellCommand** — 組み込みコマンドの実行
2. **EnvAssign** — 環境変数の代入
3. **Pipeline** — 外部コマンドの実行（パイプライン含む）

## ShellCommand（組み込みコマンド）

組み込みコマンドは現在のシェルプロセス内で直接実行される。パイプラインの一部としては使用できない。

```txt
ShellCommand ::= ShellCommandKind (CommandPart)*
```

コマンド名がクォートなし文字列として解析され、組み込みコマンド名と一致した場合に`ShellCommand`として扱われる。

## Pipeline（外部コマンド実行）

外部コマンドの実行を表す。単一コマンドもパイプなしの`Pipeline`として扱われる。

```txt
Pipeline ::= Command (Pipe Command)*
Command  ::= TempEnv* CommandPart (CommandPart | Redirect)*
```

### CommandPart

コマンド名・引数は `CommandPart` として表現される。

```txt
CommandPart ::= SimpleExpr | UnquotedString
```

`SimpleExpr`（部分式）が先に試行され、マッチしなければ`UnquotedString`（クォートなし文字列）として解析される。

### 一時環境変数（TempEnv）

コマンド実行時のみ有効な環境変数を `:=` で設定できる。コマンドの前に複数指定可能。

```txt
TempEnv ::= '$' Ident ':=' Expr
```

```asari
$VAR := "temp" command arg1 arg2
$A := "1" $B := "2" command     # 複数指定可
```

一時環境変数はそのコマンドの子プロセスにのみ渡され、シェル自体の環境変数は変更しない。値は `string` 型でなければならない（実行時エラー）。

### 外部コマンドの探索

コマンド名からフルパスを探索する。

- コマンド名にパス区切り文字が含まれる場合、直接パスとして解釈する
- それ以外は `$PATH` 内のディレクトリを順に探索する
- Windowsでは `PATHEXT` 環境変数の拡張子を自動補完する

### 引数の展開

`Value::to_args` により、値は以下のように引数に展開される。

| 値の型      | 展開結果                               |
| ----------- | -------------------------------------- |
| `string`    | そのまま1引数                          |
| `int`       | 文字列化して1引数                      |
| `float`     | 文字列化して1引数                      |
| `bool`      | 文字列化して1引数                      |
| `array<T>`  | 各要素を再帰的に展開し、複数引数に展開 |
| `option<T>` | `some`なら中身を展開、`none`なら0引数  |
| `unit`      | 0引数                                  |

### 空コマンドの扱い

コマンド名が空文字列に評価された場合、`EmptyCommand`エラーとなる。

### 終了ステータス

- Pipelineの最後のコマンドの終了コードが `$?` に設定される
- Unixではシグナルによる終了の場合、`128 + シグナル番号` が終了コードとなる

## EnvAssign（環境変数代入）

環境変数の代入はトップレベルのStatementとして記述する。

```txt
EnvAssign ::= '$' Ident '=' Expr
```

`=` の前後にはスペースが必須。

```asari
$VAR = "value"                 # 文字列を代入（set_var）
$VAR = some("value")           # option<string>のsomeも可（中身のstringをset_var）
$VAR = none                    # 削除（remove_var）
```

代入可能な値の型:

| 値の型                 | 動作                                       |
| ---------------------- | ------------------------------------------ |
| `string`               | `std::env::set_var` で設定                 |
| `option(some(string))` | 中身の `string` で `set_var`               |
| `option(none)`         | `std::env::remove_var` で削除              |
| その他                 | 実行時エラー（`EnvAssignNotStringOrNone`） |

## セミコロン

`;` は複数のStatementを1行に記述するために使用する。末尾のセミコロンは省略可能。

```asari
echo hello; echo world         # 2つのコマンドを順次実行
echo hello;                    # 末尾セミコロンは省略可能
cd /tmp; ls                    # 組み込みと外部コマンドの組み合わせ
```

## エラーハンドリング

Statementの実行時エラーは種類によって異なる動作をする。

| エラーの種類                   | 動作                             |
| ------------------------------ | -------------------------------- |
| `Exit(code)`                   | シェル全体を終了する（伝播）     |
| Pipeline内のその他エラー       | エラーメッセージを表示し次に進む |
| ShellCommand/EnvAssignのエラー | エラーメッセージを表示し次に進む |

---

# パイプ仕様

## 概要

パイプはコマンド間の標準入出力を接続する。

## 構文

```txt
Pipe ::= '|' | '|&'
```

| 構文  | 意味                   | 接続するストリーム              |
| ----- | ---------------------- | ------------------------------- |
| `\|`  | stdout パイプ          | 左のstdout → 右のstdin          |
| `\|&` | stdout + stderr パイプ | 左のstdout + stderr → 右のstdin |

## 動作

- パイプの各コマンドは並列にspawnされる
- 左コマンドのstdoutがパイプの書き込み端に接続され、右コマンドのstdinがパイプの読み取り端に接続される
- `|&` の場合、左コマンドのstderrも同じパイプの書き込み端に接続される
- 全コマンドの終了を待ち、最後のコマンドの終了ステータスが `$?` に設定される

## 例

```asari
ls | grep ".txt"               # stdoutをパイプ
ls |& grep "error"             # stdout+stderrをパイプ
cat file.txt | sort | uniq     # 3段パイプ
```

## 制約

- 組み込みコマンド（cd, exit等）はパイプラインの一部としては使用できない（`ShellCommand` と `Pipeline` はパース段階で別のStatementとして分離される）
- パイプとリダイレクトの重複チェックは検証フェーズで行われる

---

# リダイレクト仕様

## 概要

リダイレクトはコマンドの標準入出力をファイルや文字列に接続する。
リダイレクトはPipeline内の各Commandに対して指定でき、引数と混在して記述可能。

## 構文

```txt
Redirect ::= InputRedirect | OutputRedirect | MergeRedirect
```

## 入力リダイレクト

```txt
InputRedirect ::= '<' CommandPart     # ファイルから読み込み
                | '<<<' CommandPart   # ヒアストリング
```

| 構文  | 説明                        | 例                 |
| ----- | --------------------------- | ------------------ |
| `<`   | ファイルの内容をstdinに接続 | `sort < input.txt` |
| `<<<` | 式の評価結果をstdinに接続   | `cat <<< "hello"`  |

ヒアストリング（`<<<`）では、`CommandPart`が評価された結果の文字列がstdinに渡される。文字列型以外の値が渡された場合は実行時エラーとなる。

ヒアドキュメント（`<<`）は現在未実装（パーサーにコメントアウトされたコードあり）。

## 出力リダイレクト

```txt
OutputRedirect ::= '>'  CommandPart   # stdout上書き
                 | '>>' CommandPart   # stdout追記
                 | '2>'  CommandPart  # stderr上書き
                 | '2>>' CommandPart  # stderr追記
                 | '&>'  CommandPart  # stdout+stderr上書き
                 | '&>>' CommandPart  # stdout+stderr追記
```

| 構文  | 対象            | モード |
| ----- | --------------- | ------ |
| `>`   | stdout          | 上書き |
| `>>`  | stdout          | 追記   |
| `2>`  | stderr          | 上書き |
| `2>>` | stderr          | 追記   |
| `&>`  | stdout + stderr | 上書き |
| `&>>` | stdout + stderr | 追記   |

ファイルは自動的に作成される。`CommandPart`が`string`以外に評価された場合は実行時エラーとなる。

## FDマージ

```txt
MergeRedirect ::= '2>&1'   # stderrをstdoutに合流
                | '1>&2'   # stdoutをstderrに合流
```

| 構文   | 説明                             |
| ------ | -------------------------------- |
| `2>&1` | stderrをstdoutと同じ出力先に接続 |
| `1>&2` | stdoutをstderrと同じ出力先に接続 |

## 例

```asari
echo hello > output.txt              # stdoutをファイルに書き込み
echo hello >> output.txt             # stdoutをファイルに追記
command 2> error.log                 # stderrをファイルに書き込み
command &> all.log                   # stdout+stderrをファイルに書き込み
command 2>&1 | grep error            # stderrをstdoutに合流してパイプ
sort < input.txt > output.txt        # 入力と出力の同時リダイレクト
cat <<< "hello world"               # ヒアストリング
cat <<< $VAR!                        # 式の評価結果をstdinに
```

## リダイレクトとパイプの適用順序

1. パイプが先に接続される
2. その後、各コマンドのリダイレクトが左から右に順次適用される

これにより、パイプの接続をリダイレクトで上書きすることが可能。

## エラー条件

| 状況                           | 動作               |
| ------------------------------ | ------------------ |
| ファイルのオープンに失敗       | 実行時エラー       |
| リダイレクト先が文字列型でない | 実行時エラー       |
| 同一FDへの重複リダイレクト     | 検証エラー（後述） |

---

# 検証仕様

## 概要

パース後、実行前に静的な検証を行う。現在はリダイレクトの重複チェックが実装されている。

## リダイレクトの重複チェック

各Commandに対して、stdin/stdout/stderrそれぞれに対するリダイレクト元が2つ以上ある場合にエラーとする。パイプによる接続もカウントに含まれる。

| エラー            | 条件                                                 |
| ----------------- | ---------------------------------------------------- |
| `DuplicateStdin`  | 入力リダイレクトが2つ以上 or パイプ+入力リダイレクト |
| `DuplicateStdout` | stdout出力先が2つ以上 or パイプ+出力リダイレクト     |
| `DuplicateStderr` | stderr出力先が2つ以上                                |

`&>` / `&>>` はstdoutとstderrの両方にカウントされる。`|&` はstdoutとstderrの両方にカウントされる。

---

# 組み込みコマンド仕様

## 概要

組み込みコマンドはシェルプロセス内で直接実行される。外部プロセスを生成しない。

## コマンド一覧

| コマンド | 説明             |
| -------- | ---------------- |
| `cd`     | ディレクトリ変更 |
| `exit`   | シェルの終了     |

## cd

```asari
cd               # ホームディレクトリに移動
cd path          # 指定パスに移動（相対パス可）
```

- 引数なしの場合、ホームディレクトリに移動する
- 引数は1つのみ。2つ以上はエラー
- 引数は`string`型でなければエラー
- 存在しないパス、ディレクトリでないパスはエラー
- 相対パスは現在のディレクトリからの相対として解決される

## exit

```asari
exit             # 終了コード 0 で終了
exit 0           # 終了コード 0 で終了
exit 1           # 終了コード 1 で終了
```

- 引数なしの場合、終了コード0で終了する
- 引数が2つ以上の場合、警告メッセージを表示するが最初の引数で終了する
- 引数は `int` 型または `string` 型（数値文字列）を受け付ける
- `i32` の範囲外、または数値に変換できない場合は終了コード -1 で終了する

---

# リテラル仕様

## リテラルの種類

### 1. ダブルクォート文字列

**構文**: `"..."`

**特徴**:

- エスケープシーケンスが有効
- 複数行の文字列を記述可能

**例**:

```asari
"hello world"
"line1\nline2"
"path: \~not_expanded"
```

### 2. シングルクォート文字列

**構文**: `'...'`

**特徴**:

- エスケープシーケンスが有効
- ダブルクォート文字列と同等の機能

**例**:

```asari
'hello world'
'it\'s a test'
```

### 3. Raw文字列

**構文**: `r"..."` または `r#"..."#`

**特徴**:

- エスケープシーケンスが無効（バックスラッシュはそのまま保持）
- `#`の数を増やすことで、文字列内に`"`や`"#`を含めることが可能
- 開始と終了の`#`の数は一致する必要がある

**例**:

```asari
r"C:\Users\name"           → C:\Users\name
r#"say "hello""#           → say "hello"
r##"contains "#""##        → contains "#"
```

### 4. パスリテラル

**構文**: `p"..."` または `p#"..."#`

**特徴**:

- エスケープシーケンスが無効
- 評価時にチルダ展開・グロブ展開・存在確認が行われる
- `#`の扱いはRaw文字列と同様
- 評価結果は`array<string>`型（存在するパスのみの一覧）
- 存在しないパスは結果に含まれない

**例**:

```asari
p"~/Documents"             → ["/home/user/Documents"]（存在する場合）
p"~/Documents"             → []（存在しない場合）
p"/home/*.txt"             → ["/home/a.txt", "/home/b.txt"]（存在するファイルのみ）
p"/no/match/*"             → []（マッチするファイルがない場合）
p#"C:\Users\*"#            → Windowsパス対応
```

**補足**:

- グロブの有無にかかわらず、存在するパスのみが結果に含まれる
- `array<string>`型のため、echoに渡すとスペース区切りで出力される
- 存在しないパスを指定する必要がある場合はクォートなし文字列のチルダ展開を使用する

### 5. クォートなし文字列

**構文**: 特殊文字・空白を含まない文字列

**特徴**:

- エスケープシーケンスが無効
- 特殊文字または空白文字で終了する
- チルダ展開が行われる（グロブ展開は行わない）
- パスの存在確認は行わない

**チルダ展開の条件**:

- 単語の先頭が`~`で始まること
- `~`の直後が`/`または単語の終端であること
- 先頭以外の`~`は展開しない

**例**:

```asari
~/Documents                → /home/user/Documents（チルダ展開）
~                          → /home/user（チルダ展開）
hello                      → hello（変化なし）
hello-world                → hello-world（変化なし）
/usr/bin/python            → /usr/bin/python（変化なし）
hello~world                → hello~world（先頭でないため展開しない）
~/new_project              → /home/user/new_project（存在しなくてもそのまま）
```

## エスケープシーケンス

Rustと同じ仕様。
ダブルクォート文字列およびシングルクォート文字列内で有効。

| シーケンス | 意味                  | 文字コード |
| ---------- | --------------------- | ---------- |
| `\n`       | 改行                  | U+000A     |
| `\r`       | キャリッジリターン    | U+000D     |
| `\t`       | 水平タブ              | U+0009     |
| `\0`       | ヌル文字              | U+0000     |
| `\\`       | バックスラッシュ      | U+005C     |
| `\"`       | ダブルクォート        | U+0022     |
| `\'`       | シングルクォート      | U+0027     |
| `\u{XXXX}` | Unicodeコードポイント | 指定値     |

### Unicodeエスケープ

- 形式: `\u{X}` から `\u{XXXXXX}`（1〜6桁の16進数）
- 有効範囲: U+0000 〜 U+10FFFF（サロゲートペアU+D800〜U+DFFFを除く）
- 無効なコードポイントはパースエラー

**例**:

```asari
"\u{41}"        → A
"\u{3042}"      → あ
"\u{1F600}"     → 😀
```

## 特殊文字

クォートなし文字列を終了させる文字:

| 文字     | 用途                            |
| -------- | ------------------------------- | ------ |
| 空白文字 | 引数区切り（Unicode空白を含む） |
| `(` `)`  | 式 / コマンド置換               |
| `{` `}`  | コマンドグループ / サブシェル   |
| `        | `                               | パイプ |
| `>` `<`  | リダイレクト                    |
| `;`      | コマンド区切り                  |
| `&`      | バックグラウンド実行（予約）    |

## 文字列の連結

リテラル同士の直接連結は許可しない。
連結には式構文を使用する。

```asari
# 無効
"hello"'world'

# 有効
("hello" + 'world')
```

## Primaryのパース優先順位

`primary`のパース時、以下の順序で試行する:

1. `$` → `$(` ならコマンド置換、`$?`等なら特殊変数、それ以外は環境変数
2. `@` → シェル変数
3. `'` → シングルクォート文字列
4. `"` → ダブルクォート文字列
5. `r` → Raw文字列を試行、失敗時はクォートなし文字列
6. `p` → パスリテラルを試行、失敗時はクォートなし文字列
7. `(` → 括弧式（完全式への入口）
8. `[` → 配列リテラル
9. その他（括弧内のみ） → 数値、bool、none/some/unit

**補足**: コメント（`#`）の判定は `primary` ではなく `command_line` パーサーのレベルで行われる。

## エラー条件

| 状況                        | 動作                                   |
| --------------------------- | -------------------------------------- |
| 閉じられていないクォート    | 未定義（現状はクォートなしとして解析） |
| 無効なエスケープシーケンス  | パースエラー                           |
| 無効なUnicodeコードポイント | パースエラー                           |

---

# 変数仕様

## 概要

Asariでは環境変数とシェル変数を明確に区別する。

| 種類       | 接頭辞 | スコープ             | 継承       |
| ---------- | ------ | -------------------- | ---------- |
| 環境変数   | `$`    | プロセス間で共有     | 子プロセス |
| シェル変数 | `@`    | シェルプロセス内のみ | なし       |

**設計意図**: `$`（外部/環境）と`@`（内部/シェル）という対比により、変数の種類を直感的に識別できる。

## `@` 記号の二重用途

`@`はシェル変数の接頭辞と後置長さ演算子の両方に使用される。これらは**位置**によって完全に区別される。

| 位置           | 解釈               | 例         |
| -------------- | ------------------ | ---------- |
| primary位置    | シェル変数の接頭辞 | `@counter` |
| 式の後（後置） | 長さ演算子         | `@name@`   |

パーサはprimary解析とpostfix解析を別関数で行うため、構文上の曖昧さは発生しない。

```asari
@name              # シェル変数 name の参照
@name@             # シェル変数 name の長さ（後置 @ は長さ演算子）
$PATH!@            # 環境変数 PATH を unwrap してから長さ取得
```

## 変数名の規則

Rustの識別子規則に準拠する（Unicode Standard Annex #31）。

**形式**:

- `XID_Start XID_Continue*`
- `_ XID_Continue+`（アンダースコア単体は不可）

**有効な例**:

```asari
$PATH
$my_var
$変数
$カウンタ
@_private
@counter2
```

**無効な例**:

```asari
$2var          # 数字開始
$_             # アンダースコア単体
$my-var        # ハイフン不可
```

**補足**:

- 大文字小文字は区別する
- ひらがな、カタカナ、漢字等のUnicode文字を許可
- 識別子はNFC（Normalization Form C）で正規化される

## 環境変数

### 参照

環境変数の参照は常に`option<string>`型を返す。

| 構文             | 意味                                         | 戻り値の型       |
| ---------------- | -------------------------------------------- | ---------------- |
| `$VAR`           | 参照                                         | `option<string>` |
| `$VAR?`          | 存在確認（is_some）                          | `bool`           |
| `$VAR!`          | unwrap（存在しなければエラー）               | `string`         |
| `$VAR ^ default` | unwrap_or（存在しなければデフォルト値）      | `string`         |
| `$VAR!@`         | 文字列長（unwrap込み、存在しなければエラー） | `int`            |

**例**:

```asari
echo $PATH!                    # 存在しなければエラー
echo $EDITOR ^ "vim"           # なければ "vim"
echo $EDITOR^"vim"             # スペースなしも可
if $HOME? { echo "HOME is set" }  # 存在確認
echo $PATH!@                   # PATHの文字列長
```

### 代入

環境変数の代入はStatementレベルで行う。`=` の前後にはスペースが必須。

```asari
$VAR = "value"                 # 新規作成または更新（自動的にsome()でラップ）
$VAR = none                    # 削除（remove_var）
```

代入の右辺は完全式（Expr）として評価される。代入可能な値は `string`、`option(some(string))`、`option(none)` のいずれかであり、それ以外の型は実行時エラーとなる。

### 一時的な環境変数

コマンド実行時のみ有効な環境変数は`:=`で設定する。

```asari
$VAR := "temp" command arg1 arg2
```

一時環境変数はそのコマンドの子プロセスにのみ渡され、シェル自体の環境変数は変更しない。値は `string` 型でなければならない。

## シェル変数

### 参照

シェル変数は宣言時の型を保持する。

| 構文             | 意味                             |
| ---------------- | -------------------------------- |
| `@VAR`           | 参照（変数の型）                 |
| `@VAR?`          | Option型の存在確認（is_some）    |
| `@VAR!`          | Option型のunwrap                 |
| `@VAR ^ default` | Option型のunwrap_or              |
| `@VAR@`          | 長さ（文字列長または配列要素数） |

### 代入

```asari
@VAR = value                   # 型推論
@VAR: type = value             # 型明示
@VAR = none                    # Option型に none を代入、または変数削除
```

### 型明示

型推論できない場合（空配列など）は型明示が必須。

```asari
@nums: array<int> = []         # 空の整数配列
@result: option<string> = none # 明示的なOption型
```

## 特殊変数

| 変数 | 型            | 意味                              |
| ---- | ------------- | --------------------------------- |
| `$?` | `int`         | 直前コマンドの終了ステータス      |
| `$$` | `int`         | シェル自身のPID                   |
| `$!` | `option<int>` | 最後のバックグラウンドジョブのPID |
| `$@` | `string`      | シェル名またはスクリプト名        |

**補足**:

- `$@`は特殊変数であり、シェル変数`@`とは異なる。`$`に続く`@`が特殊変数として解釈される
- スクリプト引数は通常の関数引数として受け取る（後述）
- `$0`〜`$9`、`$#`単体、`$*`は使用しない

## 型システム

### 基本型

| 型名     | 説明                | リテラル例             |
| -------- | ------------------- | ---------------------- |
| `string` | 文字列              | `"hello"`, `'world'`   |
| `int`    | 整数（64bit）       | `42`, `-1`             |
| `float`  | 浮動小数点（64bit） | `3.14`, `-0.5`         |
| `bool`   | 真偽値              | `true`, `false`        |
| `unit`   | ユニット型          | （代入式の戻り値など） |

### 型表記

型を明示的に記述する構文。変数の型注釈（`@VAR: Type`）および型変換（`as Type`）で使用する。

**構文**:

```txt
Type ::= "string" | "int" | "float" | "bool" | "unit"
       | "array" "<" Type ">"
       | "option" "<" Type ">"
```

**特徴**:

- `<` `>` の前後にスペースを許可する
- ネストを許可する
- 大文字小文字を区別する（すべて小文字）

**例**:

```asari
# 基本型
@x: int = (42)
@s: string = "hello"

# 複合型
@nums: array<int> = [1, 2, 3]
@result: option<string> = none

# ネスト
@matrix: array<array<int>> = [[1, 2], [3, 4]]
@nested: option<array<int>> = some([1, 2])

# スペース許可
@a: array< int > = []
@b: option< array< string > > = none

# as での複合型使用
(none as option<int>)
([] as array<string>)
```

**無効な例**:

```asari
@x: Int = (42)         # 大文字は不可
@x: integer = (42)     # 略称は不可
@x: Array<int> = []    # 大文字は不可
```

### 複合型

| 型名        | 説明           | リテラル例         |
| ----------- | -------------- | ------------------ |
| `array<T>`  | 配列（同一型） | `[1, 2, 3]`        |
| `option<T>` | Option型       | `some(42)`, `none` |

### パスリテラルの型

パスリテラルは `array<string>` 型として評価される。

| パスリテラル     | 結果の型        |
| ---------------- | --------------- |
| `p"~/Documents"` | `array<string>` |
| `p"/tmp/*.log"`  | `array<string>` |

### 型変換（`as`演算子）

| 変換元 → 変換先         | 動作                                          |
| ----------------------- | --------------------------------------------- |
| `string → int`          | パース（失敗時エラー）                        |
| `string → float`        | パース（失敗時エラー）                        |
| `string → bool`         | `"true"` / `"false"` のみ（それ以外はエラー） |
| `int → string`          | 文字列化                                      |
| `int → float`           | 変換                                          |
| `float → string`        | 文字列化                                      |
| `float → int`           | 切り捨て（非有限数はエラー）                  |
| `bool → string`         | 文字列化                                      |
| `array<T> → array<U>`   | 各要素を再帰的に変換                          |
| `option<T> → option<U>` | 中身を変換（noneはそのまま）                  |
| その他                  | 実行時エラー                                  |

## 環境変数との関係

環境変数は常に`option<string>`として取得される。
代入時は値が自動的に`some()`でラップされる。

```asari
$MY_VAR = "hello"      # 内部的には some("hello")
echo $MY_VAR!          # unwrapして "hello"
echo $MY_VAR ^ "default"  # 存在すれば値、なければ "default"
if $MY_VAR? { ... }    # 存在確認
```

## 展開のコンテキスト

| コンテキスト     | 変数展開 | チルダ展開 | グロブ展開+存在確認 |
| ---------------- | -------- | ---------- | ------------------- |
| クォートなし     | する     | する       | しない              |
| ダブルクォート   | しない   | しない     | しない              |
| シングルクォート | しない   | しない     | しない              |
| raw文字列        | しない   | しない     | しない              |
| パスリテラル     | しない   | する       | する                |

**補足**:

- 変数展開はクォートなしの場合のみ行われる
- チルダ展開はクォートなし文字列とパスリテラルで共通の関数を使用する
- パスリテラルのみグロブ展開と存在確認が行われ、結果は`array<string>`型となる
- クォートなし文字列のチルダ展開はパスの存在確認を行わない

## 単語分割

変数展開後の単語分割は行わない。

```asari
@var = "a b c"
echo @var              # 1引数として "a b c" を渡す
```

配列の場合は複数引数として展開される。

```asari
@files = ["a.txt", "b.txt", "c.txt"]
rm @files              # rm a.txt b.txt c.txt と同等
```

## 未定義変数

未定義変数の参照はエラーとする。

```asari
echo @undefined        # エラー: 未定義の変数 'undefined'
```

環境変数は`option<string>`として取得されるため、`$VAR?`で存在確認を行うことで安全にアクセスできる。

## 代入式の戻り値

代入式は`unit`型を返す。値は返さない。

```asari
echo (@x = 10)         # 何も出力しない
@y = (@x = 10)         # @y は unit 型、実質的に消える
```

---

# 式仕様

## 概要

Asariの式は「部分式」と「完全式」の二層構造を持つ。

| 種類   | 使用可能なコンテキスト          | 使用可能な演算子              |
| ------ | ------------------------------- | ----------------------------- |
| 部分式 | コマンド引数                    | 後置演算子、配列アクセス、`^` |
| 完全式 | 括弧内 `()`、if/for/while条件部 | 全ての演算子                  |

## 部分式（Simple Expression）

コマンド引数として使用できる式。後置演算子は変数に直接付ける（スペースなし）。

### 構文

```txt
SimpleExpr ::= Primary Postfix*

Primary ::= QuotedString         # クォート文字列
          | Variable             # 変数
          | ArrayLiteral         # 配列リテラル
          | PathLiteral          # パスリテラル
          | CommandSubst         # コマンド置換
          | '(' Expr ')'         # 括弧（完全式への入口）

Postfix ::= '!'                  # unwrap
          | '?'                  # is_some
          | '@'                  # 長さ
          | '[' Expr ']'         # 配列アクセス
          | '^' SimpleExpr       # unwrap_or（右結合、右辺にも後置演算子適用可）
```

**補足**: 後置演算子はPrimaryの後に連鎖可能。括弧式や配列リテラルの後にも適用できる。

### 後置演算子のスペース規則

後置演算子は変数に直接付ける必要がある。スペースを挟むと別トークンとして解釈される。

```asari
$VAR!              # unwrap（演算子として解釈）
$VAR !             # $VAR と "!" の2引数（"!" はリテラル）

@arr[0]            # 配列アクセス
@arr [0]           # @arr と配列リテラル [0] の2引数
```

### `^` 演算子の右辺

`^` の右辺には以下を許可する。クォートなし文字列は不許可。

| 構文               | 許可 | 例                  |
| ------------------ | ---- | ------------------- |
| クォート文字列     | ✓    | `$VAR^"default"`    |
| 変数               | ✓    | `$VAR^$OTHER`       |
| 配列リテラル       | ✓    | `$VAR^[1, 2]`       |
| パスリテラル       | ✓    | `$VAR^p"~/file"`    |
| 括弧式             | ✓    | `$VAR^(1 + 2)`      |
| クォートなし文字列 | ✗    | `$VAR^default` はNG |

### 後置演算子の連鎖

後置演算子は連鎖可能。構文解析時には全て許可し、型チェック時にエラーを判定する。
括弧式や配列リテラルの後にも後置演算子を適用可能。

```asari
$VAR!@             # unwrap してから長さ取得
$VAR^$OTHER!       # $VAR ^ ($OTHER!) と解釈
$VAR^@arr[0]       # $VAR ^ (@arr[0]) と解釈
$VAR^$A^$B         # $VAR ^ ($A ^ ($B)) と解釈（右結合）

# 括弧式への後置演算子適用
("ab" + "cd")@     # 4（括弧式の結果の長さ）
[1, 2, 3]@         # 3（配列リテラルの長さ）
@A^($B + "str")@   # @A ^ (($B + "str")@) と解釈

# パスリテラルへの後置演算子適用
p"~/Documents"@    # 存在すれば1、しなければ0
p"~/Documents"[0]! # 存在確認済みパスの最初の要素をunwrap
p"/tmp/*.log"@     # マッチしたファイル数
```

**型チェック時のエラー例**:

```asari
$VAR?!             # bool に ! は無効
$VAR!?             # T に ? は無効（Option<T>でない場合）
$VAR@@             # int に @ は無効
```

### 使用例

```asari
echo $PATH!                    # 環境変数をunwrap
echo $EDITOR^"vim"             # デフォルト値付き
process @args[0]!              # 配列の最初の要素をunwrap
echo @name@                    # 文字列長
rm @files                      # 配列を複数引数として展開
echo ("ab" + "cd")@            # 括弧式の結果の長さ（4）
echo [1, 2, 3]@                # 配列リテラルの長さ（3）
echo @A^($B + "str")@          # unwrap_orの右辺に後置演算子適用
rm p"/tmp/*.log"               # 存在するログファイルを削除
echo p"/tmp/*.log"@            # マッチしたファイル数

# パスリテラルとクォートなし文字列の使い分け
mkdir ~/new_dir                # クォートなし: 存在しなくてもOK
rm p"~/old_dir/*"              # パスリテラル: 存在するファイルのみ
```

## 完全式（Full Expression）

括弧内、および制御構文（if/for/while）の条件部で使用できる式。

### 構文

```txt
Expr       ::= Unwrap_or
Unwrap_or  ::= LogicalOr ('^' LogicalOr)*
LogicalOr  ::= LogicalAnd ('||' LogicalAnd)*
LogicalAnd ::= Equality ('&&' Equality)*
Equality   ::= Comparison (('==' | '!=') Comparison)*
Comparison ::= Additive (('<' | '>' | '<=' | '>=') Additive)*
Additive   ::= Multiplicative (('+' | '-') Multiplicative)*
Multiplicative ::= Unary (('*' | '/' | '%') Unary)*
Unary      ::= '-' Unary | '!' Unary | Cast
Cast       ::= Postfixed ('as' Type)?
Postfixed  ::= Primary PostfixOp*
PostfixOp  ::= '!' | '?' | '@' | '[' Expr ']'
Primary    ::= Literal | Variable | ArrayLiteral | PathLiteral | CommandSubst | '(' Expr ')'
```

### 演算子一覧

| 演算子            | 種類 | 説明                   | 結合性 |
| ----------------- | ---- | ---------------------- | ------ | ------ | --- |
| `@`               | 後置 | 長さ取得               | 左     |
| `!`               | 後置 | unwrap                 | 左     |
| `?`               | 後置 | is_some                | 左     |
| `[]`              | 後置 | 配列アクセス           | 左     |
| `-` (単項)        | 前置 | 負数                   | 右     |
| `!` (単項)        | 前置 | 論理否定               | 右     |
| `as`              | 中置 | 型変換                 | 左     |
| `*` `/` `%`       | 中置 | 乗算・除算・剰余       | 左     |
| `+` `-`           | 中置 | 加算・減算・文字列連結 | 左     |
| `<` `>` `<=` `>=` | 中置 | 比較                   | 左     |
| `==` `!=`         | 中置 | 等値比較               | 左     |
| `&&`              | 中置 | 論理AND                | 左     |
| `                 |      | `                      | 中置   | 論理OR | 左  |
| `^`               | 中置 | unwrap_or              | 右     |

**補足**: 剰余演算子`%`は完全式（括弧内）でのみ使用可能。シェル変数接頭辞`@`との衝突を避けるため、`%`をシェル変数に使用しない設計を採用した。

### 演算子優先順位（高い順）

1. `@` `!` `?` `[]` （後置）
2. `-` `!` （単項前置）
3. `as`
4. `*` `/` `%`
5. `+` `-`
6. `<` `>` `<=` `>=`
7. `==` `!=`
8. `&&`
9. `||`
10. `^` （最低優先度）

### スペースの扱い

完全式内ではスペースの有無で動作は変わらない。

```asari
(1+2)              # 3
(1 + 2)            # 3
($VAR!)            # unwrap
($VAR !)           # unwrap（同じ意味）
```

### 使用例

```asari
# 算術演算
(@x + 1)
(@price * @quantity)
(@total / @count)

# 比較・論理
(@age >= 18 && @age < 65)
(@status == "active" || @status == "pending")

# 文字列連結
("Hello, " + @name + "!")

# 型変換
(42 as string)
(@input as int)

# 複合型の型変換
(none as option<int>)
([] as array<string>)

# 剰余演算（完全式内のみ）
(@n % 2 == 0)

# 複合式
((@x + 1) * 2)
(@arr[0]! + @arr[1]!)

# パスリテラルとの組み合わせ
(p"~/Documents"@ > 0)       # 存在するファイル数が0より大きいか
(p"/tmp/*.log"[0] ^ "none") # 最初のマッチ結果、なければ"none"
```

## 制御構文での完全式

if/for/while文の条件部では、括弧なしで完全式を記述できる。
条件式の終端は `{` で判定する。

```asari
if @count > 10 { echo "many" }
if $HOME? && @initialized { ... }
while @index < @arr@ { ... }
for @i in 0..@count { ... }
```

## 空括弧

空の括弧 `()` は `unit` 型として解釈される。

```asari
()                 # unit型
@result = ()       # @result は unit 型
```

## 評価戦略

### 短絡評価

`&&` と `||` は短絡評価を行う。

```asari
# $VAR が存在しない場合、右辺は評価されない
$VAR? && $VAR!@ > 10

# 左辺が true なら右辺は評価されない
@found || expensive_search()
```

### 遅延評価

`^` の右辺は必要な場合のみ評価される。

```asari
# $VAR が存在すれば expensive_default() は呼ばれない
$VAR ^ expensive_default()
```

## 型チェック

型チェックは評価時に行う。型の不一致は実行時エラーとして報告される。

### 型エラーの例

```asari
(1 + "hello")          # int と string の加算は不可
(1 + 1.5)              # int と float の混合演算は不可
("hello"!)             # string に unwrap は不可
(42?)                  # int に is_some は不可
```

### 演算子と型の対応

| 演算子              | 左オペランド          | 右オペランド    | 結果型      |
| ------------------- | --------------------- | --------------- | ----------- | ------ | ------ |
| `+` `-` `*` `/` `%` | `int`                 | `int`           | `int`       |
| `+` `-` `*` `/` `%` | `float`               | `float`         | `float`     |
| `+`                 | `string`              | `string`        | `string`    |
| `<` `>` `<=` `>=`   | `int` または `float`  | 同型            | `bool`      |
| `<` `>` `<=` `>=`   | `string`              | `string`        | `bool`      |
| `==` `!=`           | 任意の型 T            | 同型 T          | `bool`      |
| `&&` `              |                       | `               | `bool`      | `bool` | `bool` |
| `!` (後置)          | `option<T>`           | -               | `T`         |
| `?` (後置)          | `option<T>`           | -               | `bool`      |
| `@` (後置)          | `string` / `array<T>` | -               | `int`       |
| `[]`                | `array<T>`            | `int`           | `option<T>` |
| `^`                 | `option<T>`           | `T`             | `T`         |
| `as`                | 変換元型              | -               | 変換先型    |
| `-` (単項)          | -                     | `int` / `float` | 同型        |
| `!` (単項)          | -                     | `bool`          | `bool`      |

### 整数演算のオーバーフロー

整数の算術演算（`+`, `-`, `*`, `/`, `%`）はオーバーフローチェック付きで実行される。オーバーフローが発生した場合は実行時エラーとなる。

## 関数呼び出し（レベル2で実装予定）

式の世界ではビルトイン関数を `fn(args)` 構文で呼び出せるようにする予定。
これにより、式の世界で型付きの構造化データを扱えるようになる。

```asari
# レベル2で実装予定
(ls("src/"))                   # → Vec<FileInfo>（型付き）
(len("hello"))                 # → 5
(1 + len("hello"))             # → 6
```

設計指針:

- 式の世界 `()` = 型付き・構造化データ（`fn(args)` 構文）
- コマンドの世界 `$()` = テキストストリーム（空白区切り構文）

```txt
┌─────────────────────────────────────────┐
│  式の世界 (...)                          │
│  型付き・構造化データ                      │
│  (ls("src/")) → Vec<FileInfo>           │
│  (1 + 2)      → i64                     │
├─────────────────────────────────────────┤
│  コマンドの世界 $()                       │
│  テキストストリーム                        │
│  $(ls src/)  → String (stdout)          │
│  $(grep foo bar.txt | wc -l) → String   │
└─────────────────────────────────────────┘
```

## 配列

### リテラル

```asari
[1, 2, 3]              # array<int>
["a", "b", "c"]        # array<string>
[]                     # エラー（型明示が必要）
```

### 要素アクセス

```asari
@nums[0]               # 最初の要素（option<T>）
@nums[-1]              # 最後の要素（option<T>）
@nums[100]             # 範囲外（none）
```

### 要素更新

```asari
@nums[0] = 10          # 最初の要素を更新
```

### 配列操作

```asari
@nums + 7              # 末尾に追加した新しい配列
@nums@                 # 要素数
```

### 配列の出力

`array<string>`型の値をコマンド引数に渡すと複数引数として展開される。
echoに渡した場合はスペース区切りで出力される。

```asari
@files = ["a.txt", "b.txt", "c.txt"]
rm @files              # rm a.txt b.txt c.txt と同等
echo @files            # a.txt b.txt c.txt と出力

# パスリテラルも同様（存在するファイルのみ）
echo p"~/Documents"    # /home/user/Documents と出力（存在する場合）
echo p"/tmp/*.log"     # /tmp/a.log /tmp/b.log と出力（存在するファイルのみ）
```

## Option型

### リテラル

```asari
some(42)               # option<int>
none                   # option<unknown>
```

### 操作

| 構文             | 意味      | 備考            |
| ---------------- | --------- | --------------- |
| `@opt?`          | is_some   | `bool`を返す    |
| `@opt!`          | unwrap    | noneならエラー  |
| `@opt ^ default` | unwrap_or | noneならdefault |

---

# コマンド置換仕様

## 概要

`$(...)` は内部のコマンドを実行し、そのstdout出力を `string` 型の値として返す。
コマンド置換はPrimaryの一種であり、部分式・完全式の両方で使用可能。

## 構文

```txt
CommandSubst ::= '$(' CommandLine ')'
```

**補足**: コマンド置換内は `CommandLine` として解析されるため、セミコロンで区切られた複数のStatementや、パイプラインを含むことができる。

## 戻り値

- 常に `string` 型
- stdoutの末尾の改行（`\n`, `\r`）は除去される（bashと同じ挙動）
- stderrは親プロセスのstderrにそのまま出力される

## 実装方式

コマンド置換は自己呼出し（substモード）方式で実装されている。

```txt
eval_primary(CommandSubst(cmd))
  1. SubstPayload { command, context } を構築
  2. ペイロードをJSONにシリアライズし、一時ファイルに保存
  3. 自分自身（asari）を `subst` サブコマンド + ペイロードパスで起動
     - stdin: inherit（親のstdinをそのまま渡す）
     - stdout: piped（パイプでキャプチャ）
     - stderr: inherit（親のstderrにそのまま出力）
  4. 子プロセスの終了を待ち、stdoutを読み取り
  5. 末尾の改行を除去して string 型として返す
  6. 子プロセスの終了ステータスを $? に設定
  7. 一時ファイルはDropで自動削除される
```

substモード側の処理:

```txt
asari subst <payload_path>
  1. 一時ファイルからペイロードをデシリアライズ
  2. execute_command_line(command, context) を実行
  3. stdoutへの出力が親プロセスにパイプで返される
```

この方式の利点:

- 完全なプロセス分離によりコンテキストの独立性が自然に保証される
- ビルトインコマンドも外部コマンドも統一的にstdoutがキャプチャされる
- ペイロードの一時ファイルはDrop実装により確実に削除される

## コンテキストの扱い

コマンド置換はbashのサブシェルに近い独立スコープで実行される。

- 置換開始時に `Context` をcloneし、ペイロードに含めて子プロセスに渡す
- 置換内で `$?` は独立して更新される（置換内の複数コマンドで `$?` を参照可能）
- 子プロセスの終了ステータスが、呼び出し元の `$?` に設定される
- シェル変数 (`@`) への変更は子プロセス内のみで行われ、親には影響しない

```asari
true
echo $(false; echo $?)   # → "1"（置換内で$?が独立更新される）
echo $?                  # → 0（echoの終了ステータス）

@x = "before"
echo $(@x = "inside")
echo @x                  # → "before"（置換内の変更は親に影響しない）
```

## ネスト

コマンド置換はネスト可能。

```asari
echo $(echo $(echo hello))    # "hello"
```

## パイプラインとの組み合わせ

コマンド置換内でパイプラインを使用可能。

```asari
@count = $(cat file.txt | wc -l)
```

## 例

```asari
# 基本的な使用
echo $(echo hello)             # "hello"
@date = $(date)                # 現在日時を取得

# 式の中での使用
echo ("Result: " + $(calculate))

# コマンド引数としての使用
cat $(which asari)             # asariのパスを取得して表示

# 後置演算子との組み合わせ
echo $(whoami)@                # ユーザー名の文字数
```

## 空コマンドの扱い

コマンド名が空文字列に評価された場合、そのコマンドは何も実行せずスキップする。

```asari
$(cmd /C "exit 42")            # 空文字列が返る → コマンドとして実行されない
$("")                          # 空文字列 → スキップ
```

## エラー条件

| 状況                   | 動作                                              |
| ---------------------- | ------------------------------------------------- |
| 閉じられていない `$(`  | パースエラー                                      |
| 内部コマンドの実行失敗 | 実行時エラー                                      |
| stdoutがUTF-8でない    | 実行時エラー                                      |
| 内部でexitが呼ばれた   | 置換を中断し空文字列を返す、`$?`にexit codeを設定 |
| コマンドが非ゼロ終了   | 末尾改行を除去した文字列を返す、`$?`にcodeを設定  |
| コマンド名が空文字列   | 何もせずスキップ                                  |

---

# コメント仕様

## 概要

Asariでは行コメントをサポートする。複数行コメントとシバンは将来的にサポート予定。

## 行コメント

**構文**: `#` 以降が行末までコメントとなる

**実装**: `command_line` パーサーのレベルで、全Statementのパース後にオプショナルなコメントとして解析される。

**例**:

```asari
# 行頭コメント
echo hello  # インラインコメント
echo "hello # world"   # クォート内はリテラル
echo hello; echo world # セミコロン後のコメント
```

## 複数行コメント（未実装）

**構文**: `#| ... |#`

**特徴**:

- ネスト可能
- 複数行にまたがることができる

**例**:

```asari
#|
複数行の
コメント
|#

echo #| インライン複数行コメント |# hello

#| ネスト #| 内側 |# 外側 |#
```

**注意**: この機能は仕様として定義されているが、現在のコードには未実装。

## シバン（未実装）

**構文**: `#!` で始まる行（スクリプトの1行目のみ有効）

**特徴**:

- スクリプトファイルの1行目でのみ特別扱い
- インタプリタのパスを指定する
- 対話モードでは通常のコメントとして扱う

**例**:

```asari
#!/usr/bin/env asari
echo "Hello from Asari"
```

**注意**: スクリプト実行機能とともに将来実装予定。

## エラー条件

| 状況                              | 動作                         |
| --------------------------------- | ---------------------------- | ---------------------- |
| `$#`                              | パースエラー（無効な変数名） |
| 閉じられていない複数行コメント `# | `                            | パースエラー（未実装） |

---

# アーキテクチャ

## モジュール構成

| モジュール           | 責務                                     |
| -------------------- | ---------------------------------------- |
| `parse`              | 入力文字列のパース（winnowベース）       |
| `parse::command`     | CommandLine/Statement/Pipeline等のパース |
| `parse::expr`        | 完全式（Full Expression）のパース        |
| `parse::simple_expr` | 部分式（Simple Expression）のパース      |
| `parse::literal`     | リテラル（文字列・Raw・パス等）のパース  |
| `parse::error`       | パースエラーの定義と表示                 |
| `parse::tools`       | パースユーティリティ（ident, space等）   |
| `eval`               | 式の評価、Context管理                    |
| `exec`               | コマンド実行、パイプ・リダイレクト処理   |
| `check`              | 静的検証（リダイレクト重複チェック等）   |
| `shell_command`      | 組み込みコマンドの定義と実行             |
| `value`              | 値の型定義（Value, Type）と型変換        |
| `payload`            | コマンド置換のペイロード管理             |
| `cli`                | CLIインターフェース（clap）              |

## 実行フロー

```txt
入力文字列
  → parse::parse_shell_command()     # パース
  → check::check_command_line()      # 検証
  → exec::Shell::execute()           # 実行
     → exec::execute_command_line()
        → exec::execute_statement()  （各Statementを順次実行）
           ├── ShellCommand → shell_command::run()
           ├── Pipeline     → exec::execute_pipeline()
           └── EnvAssign    → eval::eval_expr() + std::env::set_var/remove_var
```

## CLIモード

Asariは2つのモードで動作する。

| モード       | 説明                               | 起動方法                     |
| ------------ | ---------------------------------- | ---------------------------- |
| シェルモード | 対話的なシェル                     | `asari`（引数なし）          |
| substモード  | コマンド置換の子プロセスとして実行 | `asari subst <payload_path>` |

## 外部クレート

| クレート     | 用途                      |
| ------------ | ------------------------- |
| `winnow`     | パーサーコンビネータ      |
| `serde`      | AST/Contextのシリアライズ |
| `serde_json` | JSONフォーマット          |
| `clap`       | CLI引数パース             |
| `anyhow`     | エラーハンドリング        |
| `dirs`       | ホームディレクトリの取得  |
